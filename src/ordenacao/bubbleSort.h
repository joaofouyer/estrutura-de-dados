#include "../lista/lista.h"

#ifndef ESTRUTURA_DE_DADOS_BUBBLESORT_H
#define ESTRUTURA_DE_DADOS_BUBBLESORT_H

Vetor BubbleSort(Vetor);    // Retorna um vetor ordenado e preserva o vetor antigo.

void BubbleSort2(Vetor *); // Modifica o vetor que foi passado por referência.

#endif
